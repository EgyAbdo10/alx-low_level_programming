#include "main.h"
/**
 * add - print the sign of the passed number
 * Return: - result
 * @a: the number passed
 * @b: the second number passed
 */
int add(int a, int b)
{
int res = a + b;
return (res);
}
