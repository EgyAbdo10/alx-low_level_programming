#include "main.h"
/**
 * _strlen - retest the number to 98
 * @s: the first number you want to swap
 * Return: void
 */
int _strlen(char *s)
{
int length = 0;
while (s[length] != '\0')
{
length++;
}
return (length);
}
