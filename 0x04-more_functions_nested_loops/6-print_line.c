#include "main.h"
/**
 * print_line - check the letter case
 * @n: second number
 * Return: 1 if upeer and 0 if lower
 */

void print_line(int n)
{
int i;
if (n > 0)
{
for (i = 0; i < n; i++)
{
_putchar(95);
}
}
_putchar('\n');
}
